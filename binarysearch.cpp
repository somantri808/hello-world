#include <iostream>
using namespace std;

int main () {
	int L [8], X, k, j, i;
	for (int L=1; L<=8; L++) {
		L [i] = i;
		}
		cin >> X;
		L [k] = (i + j) / 2;
		if (L [k] == X) {
		cout << "ditemukan";
		}
		else {
		if (L [k] > X)
		return 0;
}