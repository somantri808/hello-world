#include <iostream>
using namespace std;

int main () {
	int D [6] = {1, 2, 3, 4, 5, 6},cari;
	int S [6];
	//bool ketemu;
		for (int i=0; i<6; i++) {
		cout << " 1 2 3 4 5 6 " << endl;
		cin >> S [i];
		}
		cout << "\nmasukan nilai yang dicari : ";
		cin >> cari;
			for (int i=0; i<6; i++) {
			if (cari == S [6]) {
			cout << "data ditemukan" << endl;
			if else (cari == S > [6]) {
			cout << "data tidak ditemukan" << endl;
			}
			}
		}
		return 0;
	}